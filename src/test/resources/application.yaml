spring:
  data:
    mongodb:
      uri: mongodb://172.19.0.1:27017/test
      database: test
    redis:
      host: redis
      port: 6379
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL;DATABASE_TO_UPPER=false
    username: sa
    password:

  h2:
    console:
      enabled: true

  cloud:
    aws:
      s3:
        endpoint: http://localhost:4566
        accelerate-mode-enabled: true
        bucket-name: airbnb-clone-be-bucket
        access-key: asd
        secret-key: asd

      region:
        static: ap-northeast-2
  sql:
    init:
      mode: never
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG
